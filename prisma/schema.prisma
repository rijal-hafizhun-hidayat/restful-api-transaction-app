// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model m_barang {
  id         Int      @id @default(autoincrement())
  kode       String   @db.VarChar()
  nama       String   @db.VarChar()
  harga      Decimal  @db.Decimal()
  created_at DateTime @default(now()) @db.Timestamp()
  updated_at DateTime @updatedAt @db.Timestamp()
}

model m_costumer {
  id         Int      @id @default(autoincrement())
  kode       String   @db.VarChar()
  nama       String   @db.VarChar()
  telp       String   @db.VarChar()
  created_at DateTime @default(now()) @db.Timestamp()
  updated_at DateTime @updatedAt @db.Timestamp()
  t_sales    t_sales?
}

model t_sales {
  id          Int      @id @default(autoincrement())
  kode        String   @db.VarChar()
  tgl         DateTime @db.Timestamp()
  costumer_id Int      @unique @db.Integer
  subtotal    Decimal  @db.Decimal()
  diskon      Decimal  @db.Decimal()
  ongkir      Decimal  @db.Decimal()
  total_bayar Decimal  @db.Decimal()
  created_at  DateTime @default(now()) @db.Timestamp()
  updated_at  DateTime @updatedAt @db.Timestamp()

  costumer m_costumer @relation(fields: [costumer_id], references: [id], onUpdate: Cascade, onDelete: Cascade)
}
